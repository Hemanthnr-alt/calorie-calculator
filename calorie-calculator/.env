# Vite dev server proxies /api to this backend. Use 127.0.0.1 to avoid IPv6 issues on Windows.
VITE_DEV_PROXY_TARGET=http://127.0.0.1:5002

# Optional: override API base when not using the proxy (production builds).
# VITE_API_BASE_URL=https://your-api.example.com/api
